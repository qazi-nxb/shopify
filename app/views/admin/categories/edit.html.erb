<%= form_with(model: [:admin, @admin_category]) do |form| %>
  <% if @admin_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_category.errors.count, "error") %> prohibited this admin_product from being saved:</h2>

      <ul>
        <% @admin_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :title %>
    <%= form.text_field :title, class: 'form-control'%>
  </div>
  <div class="form-group">
    <%= form.label :tag_list %>
    <%= form.text_field :tag_list , class: 'form-control'%>
  </div>
  <div class="form-group">
    <%= form.submit  class:"btn btn-success"%>
  </div>
<% end %>
<%= link_to 'Show', admin_category_path(@admin_category) , class: "btn btn-warning btn-sm"%> |
<%= link_to 'Back', admin_categories_path, class: "btn btn-primary btn-sm" %>

